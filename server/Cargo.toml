[package]
name = "dandelion_server"
version = "0.1.0"
edition = "2021"

[package.metadata.cargo-machete]
ignored = ["openssl", "serde_bytes"]

[dependencies]
machine_interface = { path = "../machine_interface", features = ["bytes_context"] }
dandelion_commons = { path = "../dandelion_commons" }
dispatcher = { path = "../dispatcher" }
multinode = { path = "../multinode" }
futures = { version = "0.3.28", default-features=false}
tokio = { version = "1.38.2", features = ["full"] }
hyper = { version = "1.2", features = ["server", "http1", "http2"] }
hyper-util = { version = "0.1", features = ["tokio", "server-auto"] }
http-body-util = "0.1"
num_cpus = "1.16.0"
nix = { version = "0.31", features = ["sched"] }
bytes = "1.11"
log = { version = "0.4.20", features = ["serde", "release_max_level_debug"] }
env_logger = "0.10.1"
serde = "1.0.197"
serde_json = "1.0.115"
serde_bytes = "0.11"
bson = "2.9.0"
# input parsing
clap = {version = "4.5", features = ["env","derive"]}
# to make sure server can cross compile (removes dependecy on installed openssl
# requires C compiler, perl (and perl-core) and make
openssl = { version = "0.10", features = ["vendored"], optional=true}

[dev-dependencies]
assert_cmd = "2.1"
byteorder = "1.5"
serial_test = "3.1.1"
reqwest = { version = "0.12", features = ["blocking"] }

[features]
archive = []
cheri = ["dispatcher/cheri", "multinode/cheri"]
mmu = ["dispatcher/mmu", "multinode/mmu"]
kvm = ["dispatcher/kvm", "multinode/kvm"]
reqwest_io = ["dispatcher/reqwest_io", "multinode/reqwest_io"]
timestamp = ["dispatcher/timestamp"]
log_function_stdio = ["dispatcher/log_function_stdio"]
vendored-ssl = ["dep:openssl"]
blocking_queue = ["machine_interface/blocking_queue"]
